apiVersion: v1
kind: Service
metadata:
  name: frontend-service
  namespace: employee-management
spec:
  selector:
    app: frontend
  ports:
    - protocol: TCP
      port: 3001
      targetPort: 80
      nodePort: 30080
  type: NodePort  # Expose frontend for external access
